<%- include("partials/header"); -%>

    <h1>Home</h1>
    <p><%= pageParagraph %></p>
    <% posts.forEach(function(post){ %>
      <% console.log(post.title); %>
    <% }); %>

<%- include("partials/footer"); -%>
