<%- include("partials/header"); -%>

    <h1>Home</h1>
    <p><%= pageParagraph %></p>
    <% posts.forEach(function(post){ %>
      <h1><%= post.title.full %></h1>
      <p><%= post.body.trunc %><a href=<%= "/posts/" + post.title.kebab%>> Read More</a></p>
    <% }); %>

<%- include("partials/footer"); -%>
